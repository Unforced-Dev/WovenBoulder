{
  "name": "woven-boulder-surface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Woven Boulder \u2014 backed surface over the boulder vault (public civic projections + MCP). Kit deps from npm (@openparachute/surface-server ^0.1.1).",
  "scripts": {
    "test": "bun test server/",
    "typecheck": "tsc --noEmit",
    "smoke:live": "LIVE_SMOKE=1 bun scripts/live-smoke.ts"
  },
  "dependencies": {
    "@openparachute/surface": "^0.3.1",
    "@openparachute/surface-client": "^0.3.0",
    "@openparachute/surface-server": "^0.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5"
  }
}
